info:
  name: toggle status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/v1/roles/:id/toggle-status"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "3"
      type: path
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
