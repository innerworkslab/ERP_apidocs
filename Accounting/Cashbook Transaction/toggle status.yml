info:
  name: toggle status
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/v1/cashbooks/:id/toggle-status"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
