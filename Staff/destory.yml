info:
  name: destory
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/v1/staffs/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseUrl}}/v1/staffs/:id"
      method: DELETE
      params:
        - name: id
          value: "3"
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
