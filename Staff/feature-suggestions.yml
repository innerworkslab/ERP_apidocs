info:
  name: feature-suggestions
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/v1/staffs/feature-suggestions?role_id=1&department_id=1"
  params:
    - name: role_id
      value: "1"
      type: query
    - name: department_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
