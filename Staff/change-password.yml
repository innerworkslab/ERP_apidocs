info:
  name: change-password
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/v1/staffs/:id/change-password"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "old_password": "Password123!",
        "password": "NewPassword123!",
        "password_confirmation": "NewPassword123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseUrl}}/v1/staffs/:id/change-password"
      method: PATCH
      params:
        - name: id
          value: "2"
          type: path
      body:
        type: json
        data: |-
          {
            "old_password": "Password123!",
            "password": "NewPassword123!",
            "password_confirmation": "NewPassword123!"
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
