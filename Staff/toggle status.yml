info:
  name: toggle status
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/v1/staffs/:id/toggle-status"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
