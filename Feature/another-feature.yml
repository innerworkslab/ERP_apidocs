info:
  name: another-feature
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/v1/features/:id/other-features"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
