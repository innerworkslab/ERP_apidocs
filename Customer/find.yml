info:
  name: find
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/customers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseUrl}}/v1/customers/1"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
