info:
  name: destory
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/v1/customers/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseUrl}}/v1/customers/2"
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
