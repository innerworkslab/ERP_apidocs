info:
  name: destory
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/v1/customers/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
