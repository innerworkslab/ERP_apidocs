info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/v1/feature-recommendation-rules"
  body:
    type: json
    data: |-
      {
        "role_id": 1,
        "department_id": 1,
        "feature_id": 1,
        "is_default_recommended": true,
        "status": "active"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
