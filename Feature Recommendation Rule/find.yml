info:
  name: find
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/feature-recommendation-rules/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
