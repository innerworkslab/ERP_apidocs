info:
  name: update
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/v1/feature-recommendation-rules/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "role_id": 1,
        "department_id": 1,
        "feature_id": 2,
        "is_default_recommended": false,
        "status": "inactive"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
